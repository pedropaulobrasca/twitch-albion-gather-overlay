<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Albion Gather Overlay</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
  <style>
    body {
      background-color: #18181b;
      color: #e4e4e7;
      font-family: 'Inter', sans-serif;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-container {
      max-width: 400px;
      width: 100%;
      background-color: #27272a;
      border-radius: 8px;
      padding: 32px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.2);
    }
    .login-header {
      text-align: center;
      margin-bottom: 32px;
    }
    .login-header img {
      width: 64px;
      height: 64px;
      margin-bottom: 16px;
    }
    .btn-twitch {
      background-color: #9146ff;
      color: white;
      border: none;
      padding: 12px;
      font-weight: bold;
      width: 100%;
      font-size: 1.1rem;
      margin-bottom: 16px;
    }
    .btn-twitch:hover {
      background-color: #7d2df0;
      color: white;
    }
    .divider {
      text-align: center;
      margin: 20px 0;
      position: relative;
    }
    .divider::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 0;
      right: 0;
      height: 1px;
      background-color: #3f3f46;
      z-index: 1;
    }
    .divider span {
      position: relative;
      z-index: 2;
      background-color: #27272a;
      padding: 0 12px;
    }
    .alert {
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <div class="login-header">
      <i class="bi bi-controller text-purple" style="font-size: 3rem; color: #9146ff;"></i>
      <h2>Albion Gather Overlay</h2>
      <p class="text-muted">Faça login para acessar o painel</p>
    </div>
    
    <% if (error && error.length > 0) { %>
      <div class="alert alert-danger" role="alert">
        <%= error %>
      </div>
    <% } %>
    
    <a href="/auth/twitch" class="btn btn-twitch">
      <i class="bi bi-twitch me-2"></i> Entrar com Twitch
    </a>
    
    <div class="divider">
      <span>OU</span>
    </div>
    
    <div class="text-center mt-3">
      <a href="/" class="text-decoration-none text-light">Voltar para a página inicial</a>
    </div>
  </div>
</body>
</html> 